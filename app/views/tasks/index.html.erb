<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><h2><%= task.completed  ? '☑️' : '🔲' %>   <%= link_to task.title, task_path(task) %></h2></li>
  <% end %>
</ul>


<div class="nav_block">
  <%= link_to 'New', new_task_path  %>
</div>
